tampilkan("Menghubungkan ke database...")
db.hubungkan("sqlite://toko.db")

buat rute = pakai "rute.rpl"

rute.daftarkan()

buat port = 3012
tampilkan(`Server Toko API berjalan di http://localhost:3012`)
web.jalankan(port)
