[workspace]

[package]
name = "rust_lib_rpl_studio"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
runtime = { path = "../../../crates/runtime" }
flutter_rust_bridge = "=2.12.0"

[profile.release]
lto = true
opt-level = "s"
strip = true
